# DESKTOP.md — Tauri later, not instead of a TikTok fix

Owner 2026-09-05: friend PC bottlenecked on web studio during TikTok LIVE. Voice rarely read.

Decision: **fix TikTok load on the web studio first.** A Tauri wrapper of the same heavy page still chokes.

When the web studio stays light on a busy LIVE, then:
- Tauri 2 window, studio only
- GitHub Release (win + linux). No Steam.
- Same GitHub/Google session + Ko-fi plans. No second paywall until the build is actually lighter.

Do not start `src-tauri/` until owner says the friend test is still bad *after* app.js?v=14.
